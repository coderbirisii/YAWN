<UserControl x:Class="NOWT.Views.Match"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:NOWT.Controls"
             xmlns:viewmodels="clr-namespace:NOWT.ViewModels"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:converters="clr-namespace:NOWT.Converters"
             xmlns:fa="http://schemas.fontawesome.com/icons/fonts"
             xmlns:p="clr-namespace:NOWT.Properties"
             d:DataContext="{d:DesignInstance Type=viewmodels:MatchViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="754"
             d:DesignWidth="1536">
	<i:Interaction.Triggers>
		<i:EventTrigger EventName="Loaded">
			<i:InvokeCommandAction Command="{Binding PassiveLoadCheckCommand}" />
		</i:EventTrigger>
		<i:EventTrigger EventName="Closing">
			<i:InvokeCommandAction Command="{Binding StopPassiveLoadAsyncCommand}" />
		</i:EventTrigger>
	</i:Interaction.Triggers>
	<UserControl.Resources>
		<BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
		<converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
	</UserControl.Resources>
	<UserControl.Triggers>
		<EventTrigger RoutedEvent="Loaded">
			<BeginStoryboard>
				<Storyboard>
					<DoubleAnimation
						Storyboard.TargetProperty="Opacity"
						From="0.0" To="1.0" Duration="0:0:1" />
				</Storyboard>
			</BeginStoryboard>
		</EventTrigger>
	</UserControl.Triggers>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="70" />
			<RowDefinition />
		</Grid.RowDefinitions>
		<Grid>

			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="70" />
				<ColumnDefinition Width="70" />
				<ColumnDefinition Width="70" />
				<ColumnDefinition Width="70" />
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>

			<Button
				Grid.Column="0"
				Margin="7" x:Name="GoHome"
				Command="{Binding DataContext.NavigateHomeCommand, RelativeSource={RelativeSource AncestorType=Window}}"
				Template="{StaticResource ButtonTemplate}">

				<fa:ImageAwesome PrimaryColor="#007EF9" Icon="Solid_AngleLeft" Margin="10" />
			</Button>
			<Button
				Grid.Column="1"
				Margin="7"
				Command="{Binding DataContext.NavigateSettingsCommand, RelativeSource={RelativeSource AncestorType=Window}}"
				Template="{StaticResource ButtonTemplate}">

				<fa:ImageAwesome PrimaryColor="#007EF9" Icon="Solid_Gear" Margin="10" />
			</Button>
			<Button
				IsEnabled="{Binding Path=Overlay.IsBusy, Converter={StaticResource InverseBooleanConverter}}"
				Grid.Column="2"
				Margin="7"
				Command="{Binding GetMatchInfoCommand}"
				Template="{StaticResource ButtonTemplate}">
				<fa:ImageAwesome PrimaryColor="#007EF9" Icon="Solid_Rotate" Margin="10" />
			</Button>
			<ToggleButton
				Grid.Column="3" Margin="7"
				Template="{StaticResource ToggleButtonTemplate}">
				<i:Interaction.Triggers>
					<i:EventTrigger EventName="Checked">
						<i:InvokeCommandAction Command="{Binding Path=StopPassiveLoadAsyncCommand}" />
					</i:EventTrigger>
					<i:EventTrigger EventName="Unchecked">
						<i:InvokeCommandAction Command="{Binding Path=PassiveLoadAsyncCommand}" />
					</i:EventTrigger>
				</i:Interaction.Triggers>
				<fa:ImageAwesome PrimaryColor="#007EF9" Icon="Solid_Pause" Margin="10" />
			</ToggleButton>

			<TextBlock Margin="10" Grid.Column="4"
			           FontSize="20"
			           VerticalAlignment="Center"
			           FontWeight="Normal"
			           Foreground="White"
			           FontFamily="{StaticResource Nunito}"
			           Text="{x:Static p:Resources.RefreshingIn}" />
			<TextBlock Margin="10"
			           FontSize="20" Grid.Column="5"
			           FontFamily="{StaticResource Nunito}"
			           VerticalAlignment="Center"
			           FontWeight="Normal"
			           Foreground="#007EF9"
			           Text="{Binding RefreshTime}" />

			<!-- Right Side Info (Game Mode, Shard, Map) -->
			<StackPanel Grid.Column="6" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,15,0"
			            Visibility="{Binding IsMatchActive, Converter={StaticResource BooleanToVisibilityConverter}}">
				<!-- Game Mode Info -->
				<StackPanel Orientation="Horizontal" Margin="15,0">
					<Image Height="25" Width="25" Source="{Binding Path=Match.GameModeImage}" RenderOptions.BitmapScalingMode="HighQuality">
						<Image.Effect>
							<DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.5"/>
						</Image.Effect>
					</Image>
					<TextBlock FontFamily="{StaticResource Nunito}" FontSize="22" FontWeight="SemiBold" Margin="8,0,0,0"
					           Foreground="White" VerticalAlignment="Center"
					           Text="{Binding Path=Match.GameMode}">
						<TextBlock.Effect>
							<DropShadowEffect BlurRadius="3" ShadowDepth="2" Opacity="0.8"/>
						</TextBlock.Effect>
					</TextBlock>
				</StackPanel>

				<!-- Shard Info -->
				<StackPanel Orientation="Horizontal" Margin="15,0">
					<fa:ImageAwesome Icon="Solid_EarthAmericas" PrimaryColor="White" Height="22" Width="22" VerticalAlignment="Center">
						<fa:ImageAwesome.Effect>
							<DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.5"/>
						</fa:ImageAwesome.Effect>
					</fa:ImageAwesome>
					<TextBlock FontFamily="{StaticResource Nunito}" FontSize="22" FontWeight="SemiBold" Margin="8,0,0,0"
					           Foreground="White" VerticalAlignment="Center"
					           Text="{Binding Match.ServerName}">
						<TextBlock.Effect>
							<DropShadowEffect BlurRadius="3" ShadowDepth="2" Opacity="0.8"/>
						</TextBlock.Effect>
					</TextBlock>
				</StackPanel>

				<!-- Map Card -->
				<Border Margin="15,0,0,0" Width="180" Height="45">
					<Border.Effect>
						<DropShadowEffect BlurRadius="8" ShadowDepth="3" Opacity="0.5"/>
					</Border.Effect>
                    <Border.OpacityMask>
                        <VisualBrush>
                            <VisualBrush.Visual>
                                <Border Background="Black" Width="180" Height="45" CornerRadius="10"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </Border.OpacityMask>
					<Grid>
						<Image Source="{Binding Path=Match.MapImage}" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>
						<Border Background="#50000000"/>
						<TextBlock FontFamily="{StaticResource Nunito}" FontSize="24" FontWeight="Bold"
						           Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"
						           Text="{Binding Path=Match.Map}">
							<TextBlock.Effect>
								<DropShadowEffect BlurRadius="3" ShadowDepth="2" Opacity="1"/>
							</TextBlock.Effect>
						</TextBlock>
					</Grid>
				</Border>
			</StackPanel>

		</Grid>

		<Grid Grid.Row="1" Grid.Column="0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
			<ItemsControl ItemsSource="{Binding LeftPlayerList}" Margin="5">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<UniformGrid Columns="1" Margin="5" />
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>
				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<controls:PlayerControl MaxHeight="160"
						                        Margin="0 5"
						                        PlayerCell="{Binding}" />
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>

			<StackPanel Grid.Column="1" Orientation="Horizontal">
				<Separator Width="4" Margin="6 20 6 30"
				           Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Background="#181E34"
				           BorderThickness="0" />
			</StackPanel>
			<ItemsControl Grid.Column="2" ItemsSource="{Binding RightPlayerList}" Margin="5">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<UniformGrid Columns="1" Margin="5" />
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>
				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<controls:PlayerControl MaxHeight="160px"
						                        Margin="0 5"
						                        PlayerCell="{Binding}" />
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>

		</Grid>
		<controls:OverlayControl Grid.Row="1" Grid.Column="0"
		                         Style="{StaticResource VisibleAnimation}"
		                         Overlay="{Binding Overlay}"
		                         Visibility="{Binding Overlay.IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
	</Grid>
</UserControl>